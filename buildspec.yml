version: 0.2

phases:
  install:
    commands:
      - echo Installing Node dependencies...
      - npm install
  build:
    commands:
      - echo Building React app...
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build
